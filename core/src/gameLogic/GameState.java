package gameLogic;

/**This enum tracks the 5 states that the game can be in.*/
public enum GameState {
    NORMAL,
    PLACING,
    ROUTING,
    ANIMATING,
    WAITING,
	EDITING,
    CONFIRMEDIT,
	CONFIRMING,
    DESTROYING
}
